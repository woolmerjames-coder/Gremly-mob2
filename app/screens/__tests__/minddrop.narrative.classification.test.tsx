/**
 * Test: Narrative classification prevents todo conversion
 *
 * SKIPPED: V4 pipeline (MIND_DROP_V4_ENABLED) bypasses category/timing chips entirely.
 * These tests verify the old V2/V3 chip-based flow which is no longer active.
 */

describe.skip('Mind Drop Narrative Classification (V2/V3 legacy)', () => {
  it('placeholder', () => {});
});
