/**
 * SKIPPED: V4 pipeline (MIND_DROP_V4_ENABLED) has different error/offline handling.
 * These tests verify the old V2 flow which is no longer active.
 */

describe.skip('Mind Drop — Error & Offline UX (V2 legacy)', () => {
  it('placeholder', () => {});
});
