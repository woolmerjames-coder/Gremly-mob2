describe('Overlay Phase 2 — Title Sync', () => {
  describe('Compact title sync between overlay and recent drops', () => {
    test.todo('keeps recent drops listing in sync with overlay title edits');
  });

  describe('User-title override persistence', () => {
    test.todo('persists manual titles even after AI or tag automation runs');
  });
});
