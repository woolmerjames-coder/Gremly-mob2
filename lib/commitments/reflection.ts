export async function promptCommitmentReflection(commitmentId: string) {
  // Phase 10+: Show overlay prompt asking user “Why was this commitment missed?”
  // For now, just console.log
  console.log('[Commitments] Reflection placeholder for', commitmentId);
}
