/**
 * Shared entity components
 *
 * These components provide consistent rendering for entities (todos, habits, logs, lists)
 * across different parts of the app (Today page, Spaces, etc.)
 */

export {
  UnifiedEntityCard,
  type UnifiedEntityCardProps,
  type UnifiedEntityRecord,
  type EntityType,
} from './UnifiedEntityCard';
